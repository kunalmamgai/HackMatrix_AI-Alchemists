// Motion tokens — centralized durations and easings (redesign report 4.5).
// Only transitions.slow is currently used (Hero.jsx). Kept minimal until
// other components adopt these tokens.

export const transitions = {
  slow: { duration: 0.7, ease: 'easeOut' },
};
